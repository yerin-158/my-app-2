import React from "react";

const Square = (index) => {
    console.log(index);
    return (
        <button className="square">
            {/* TODO */}
        </button>
    );
}

export default Square;
